{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "type-check": {},
    "test": {
      "inputs": ["test/**/*.test.ts"],
      "dependsOn": [
        "type-check"
      ]
    },
    "build": {
      "outputs": [".build/*"]
    }
  }
}